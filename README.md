Uses the new fsnotify api to parse, filter and print Macos filesystem events in real 
Run as:
`./FSWatcher [--filter exclusion-list] [path-to-watch]`

```
    ./FSWatcher --filter filter-mac-stuff/filter.txt /;
```
